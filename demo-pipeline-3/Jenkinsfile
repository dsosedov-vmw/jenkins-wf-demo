pipeline {
    agent {
        label 'cdPipeline'
    }

    environment {
        FOO = 'Dmitrii'
    }

    stages {
        stage('Build') {
            steps {
                sh "echo 'Hello $FOO'"
            }
        }
    }
}
